server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;

  # Serve index.html for all non-file requests (history API)
  location / {
    try_files $uri /index.html;
  }

  # Health check endpoint
  location /health {
    access_log off;
    return 200 'ok';
  }
}
